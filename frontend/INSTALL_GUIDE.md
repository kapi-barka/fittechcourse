# 📦 Руководство по установке Frontend

## 🚀 Быстрая установка

```bash
cd frontend
npm install
```

## ⚠️ Если возникают ошибки

### Вариант 1: Использовать legacy-peer-deps (Рекомендуется)
```bash
npm install --legacy-peer-deps
```

### Вариант 2: Чистая установка
```bash
# Удали старые файлы
rm -rf node_modules package-lock.json
# или на Windows:
rmdir /s /q node_modules
del package-lock.json

# Установи заново
npm install
```

### Вариант 3: Использовать force (Осторожно!)
```bash
npm install --force
```

## ✅ Проверка установки

После установки проверь:

```bash
# Должен показать список установленных пакетов
npm list --depth=0

# Запусти dev сервер
npm run dev
```

Если всё работает, откройся http://localhost:3000 ✨

## 🐛 Решение проблем

### Ошибка: ERESOLVE unable to resolve dependency tree

**Решение:**
```bash
npm install --legacy-peer-deps
```

### Ошибка: Cannot find module

**Решение:**
```bash
rm -rf node_modules package-lock.json .next
npm install
```

### Ошибка: Port 3000 already in use

**Решение:**
```bash
# Используй другой порт
npm run dev -- -p 3001

# Или останови процесс на порту 3000
# Windows:
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# Linux/Mac:
lsof -ti:3000 | xargs kill -9
```

## 📝 Версии пакетов

Текущие версии в проекте:
- React: 18.3.1
- Next.js: 14.2.15
- TypeScript: 5.6.3
- ESLint: 8.57.0 (совместима с Next.js 14)
- Zustand: 5.0.0
- Tailwind CSS: 3.4.14

## 🔄 Обновление зависимостей

Для обновления всех пакетов:

```bash
npm update
```

Для обновления до последних версий (осторожно!):

```bash
npm install <package>@latest
```

## ✨ Готово!

После успешной установки запусти:

```bash
npm run dev
```

И открой http://localhost:3000 🎉

