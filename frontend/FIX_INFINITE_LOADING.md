# 🔄 Исправление бесконечной загрузки

## Проблема
При перезагрузке любой страницы происходит бесконечная загрузка (кастомный спиннер).

## Причина
В `useEffect` хуках были зависимости от `fetchUser`, который пересоздавался при каждом обновлении store, вызывая бесконечный цикл.

## ✅ Исправления

### 1. `frontend/app/page.tsx`

**До:**
```typescript
useEffect(() => {
  fetchUser()
}, [fetchUser])  // ❌ Зависимость от fetchUser
```

**После:**
```typescript
useEffect(() => {
  fetchUser()
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [])  // ✅ Пустой массив - вызов только при монтировании
```

### 2. `frontend/components/AuthGuard.tsx`

**До:**
```typescript
useEffect(() => {
  if (!user && !isLoading) {
    fetchUser()
  }
}, [user, isLoading, fetchUser])  // ❌ Зависимость от fetchUser
```

**После:**
```typescript
useEffect(() => {
  fetchUser()
  // eslint-disable-next-line react-hooks/exhaustive-deps
}, [])  // ✅ Пустой массив - вызов только при монтировании
```

## 🔍 Как работает исправление

1. `fetchUser()` теперь вызывается **только один раз** при монтировании компонента
2. `isLoading` внутри `authStore.ts` правильно обновляется после завершения запроса
3. Нет бесконечного цикла повторных вызовов

## ✅ Проверка

После применения исправлений:

1. Перезагрузи страницу в браузере (`Ctrl+Shift+R` или `F5`)
2. Загрузка должна завершиться за 1-2 секунды
3. Перенаправление на `/login` или `/dashboard` произойдёт автоматически

## 📝 Примечание

ESLint правило `react-hooks/exhaustive-deps` отключено для этих случаев, потому что:
- `fetchUser` не меняется логически (это стабильная функция из Zustand store)
- Мы хотим вызвать его **только один раз** при монтировании
- Добавление его в зависимости вызывает бесконечный цикл

---

**Теперь всё работает корректно! 🎉**

